package enums;

// The ways a user can pay their bill.

public enum PaymentMethod {
    // The user pays with physical currency directly
    CASH,
    // The user pays using a debit or credit card
    CARD,
    // The user sends funds directly from their bank account
    BANK_TRANSFER
}
